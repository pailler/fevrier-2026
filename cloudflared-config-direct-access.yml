tunnel: b19084f4-e2d6-47f5-81c3-0972662e953c
credentials-file: C:\Users\AAA\.cloudflared\cert.pem

ingress:
  # Application principale IAHome - accès direct port 3000
  - hostname: iahome.fr
    service: http://localhost:3000
  - hostname: www.iahome.fr
    service: http://localhost:3000
  
  # LibreSpeed - accès direct via le service LibreSpeed (si disponible)
  # Sinon, utiliser le port 8080 de Traefik pour le routage
  - hostname: librespeed.iahome.fr
    service: http://localhost:8080
  
  # Services externes - accès direct aux ports spécifiques
  - hostname: pdf.iahome.fr
    service: http://localhost:8080  # Via Traefik sur port 8080
  - hostname: metube.iahome.fr
    service: http://localhost:8080  # Via Traefik sur port 8080
  - hostname: psitransfer.iahome.fr
    service: http://localhost:8080  # Via Traefik sur port 8080
  - hostname: qrcodes.iahome.fr
    service: http://localhost:8080  # Via Traefik sur port 8080
  
  # Dashboard Traefik - port 8080
  - hostname: traefik.iahome.fr
    service: http://localhost:8080
  
  # Catch-all rule
  - service: http_status:404
